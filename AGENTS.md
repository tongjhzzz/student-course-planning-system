# 项目智能体协作规范

## 0. 规则优先级

执行任务时，按以下优先级遵守规则：

1. 用户在当前对话中明确提出的要求
2. 本 `AGENTS.md` 中的项目规则
3. 项目现有的代码风格、目录结构和工具链约定
4. 智能体自己的默认习惯

如果不同规则发生冲突，应说明冲突点，并优先遵守更高优先级的规则。

---

## 1. 先理解需求，再开始执行

在分析、修改代码、创建文件、运行可能影响项目的命令之前，必须先确认需求。

- 一次只能向用户提出 **一个问题**。
- 根据用户的回答继续追问。
- 只有在对用户真实目标、范围、约束和验收标准有至少 **95% 信心**时，才能开始实施或给出最终方案。
- 不要默默猜测用户意图。
- 如果一个需求存在多种合理理解，要明确说明差异，并询问用户选择哪一种。
- 如果发现用户的要求可能导致更复杂、更难维护或不符合当前项目结构的方案，应说明原因，并提出更简单的替代方案。
- 如果仍然存在关键不确定点，必须停止实施并继续询问，而不是自行决定。

对于非常简单、影响范围明确的任务，例如修正一个明确的拼写错误、替换用户指定的一行文本，可根据实际情况减少追问；但不能因此擅自扩大修改范围。

---

## 2. 编码前思考

不要假设，不要隐藏困惑，要让关键判断可见。

开始实现前，应先完成以下检查：

- 明确任务目标：用户最终想得到什么结果。
- 明确修改范围：哪些文件、模块、功能允许改动，哪些不应改动。
- 明确已有约束：语言标准、现有架构、命名习惯、依赖和运行方式。
- 明确验收标准：怎样才算任务完成，如何验证。
- 明确风险：是否会影响已有功能、数据格式、接口兼容性或构建流程。

当需求有歧义时：

- 不要私自选择一种解释后直接实现。
- 应说明可能的解释及其影响。
- 优先询问用户，而不是依据个人偏好猜测。
- 如果存在明显更简单的实现方式，应主动提出。
- 如果某些代码、注释或业务逻辑没有足够把握理解，不要随意改动或删除。

---

## 3. 简洁优先

使用能完成当前任务的最少代码、最少抽象和最少改动。

### 必须遵守

- 只实现用户明确要求的功能。
- 不添加用户没有要求的“未来扩展”“高级配置”“可插拔架构”或“通用框架”。
- 不为只使用一次的逻辑创建复杂抽象。
- 不为理论上不可能发生、且项目上下文没有要求处理的情况堆叠错误处理。
- 不因为“看起来更专业”而增加类层级、设计模式、模板封装或复杂依赖。
- 如果一个方案可以用 50 行清晰代码完成，不应写成 200 行复杂结构。
- 优先写直接、易读、适合初学者理解和维护的代码。

### 自检问题

在提交实现前，必须自问：

- 这段代码是否比任务本身更复杂？
- 是否加入了任何用户没有要求的功能？
- 是否存在只为“未来可能用到”而创建的抽象？
- 一名有经验的开发者是否会认为这里过度设计？

如果答案是“是”，应继续简化。

---

## 4. 精准修改

只修改完成当前任务所必需的内容，不顺手重构、不顺手清理。

### 修改已有代码时

- 不要修改与当前任务无关的代码、注释、格式或文件结构。
- 不要因为个人偏好重构没有问题的模块。
- 不要把局部任务扩展成全项目重写。
- 优先遵循项目既有代码风格，即使个人更喜欢另一种写法。
- 不要删除自己没有充分理解的代码、注释、配置或资源文件。
- 如果发现与当前任务无关的 bug、死代码或风格问题，只需在最终说明中指出；除非用户明确要求，否则不要修改。

### 可以清理的内容

仅当以下无用内容是**本次修改直接造成**时，才可以一并清理：

- 不再使用的 `#include`
- 不再使用的变量
- 不再使用的函数
- 不再使用的类或代码分支
- 因本次修改失效的注释
- 因本次修改生成、且不应保留的临时文件

### 最终自检

每一处修改都应能直接回答：

> 这行改动如何服务于用户当前的请求？

如果无法回答，应撤销或单独向用户说明。

---

## 5. 目标驱动执行与验证

不要只执行“动作”，要先定义可以验证的完成目标。

### 将模糊指令转换为可验证目标

| 用户表述 | 应转换为的目标 |
| --- | --- |
| “添加输入校验” | 为非法输入设计测试或手动测试场景，并使程序正确拒绝或提示。 |
| “修复这个 bug” | 先确认能稳定复现问题，再修改代码并验证问题不再出现。 |
| “优化代码” | 明确优化指标，例如运行时间、内存占用、可读性或代码重复，并验证改善结果。 |
| “重构某个模块” | 修改前后均能成功构建，并保证原有功能或测试结果不变。 |
| “新增功能” | 明确输入、处理过程、输出和异常情况，并验证核心使用流程可用。 |

### 对于多步骤任务

在开始实施前，先给出简短计划。每一步必须包含验证方式，例如：

```text
1. 阅读相关代码，确认当前数据流和调用关系
   → 验证：能说明问题发生的位置及影响范围

2. 以最小改动实现目标功能
   → 验证：项目可以成功编译

3. 运行测试或构造测试数据验证主要流程
   → 验证：结果符合用户确认的验收标准

